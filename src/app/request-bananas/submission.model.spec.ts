/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Submission} from './submission.model';

describe('Submission', () => {
  it('should create an instance', () => {
    expect(new Submission()).toBeTruthy();
  });
});
